# Only needed for the LLM-backed extractor / agent in projects/annual-report-risk-rag.
# Every test and the rule-based baseline run fully offline without it.
ANTHROPIC_API_KEY=
# Optional override (defaults to claude-opus-5)
REPORT_RAG_MODEL=claude-opus-5
